pipeline {
    agent any

    stages {
        stage('Validacion inicial') {
            steps {
                sh 'pwd'
                sh 'ls -la'
                sh 'chmod +x gradlew'
                sh './gradlew --version'
            }
        }
    }
}